<Window x:Class="FACTOVA_MessageLogViewer.ColumnSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Column Settings" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Column Settings" FontSize="18" FontWeight="Bold"/>
            <TextBlock Text="Column: Show as column | Summary: Show in summary | Hidden: Do not show"
                       Foreground="#666" FontSize="12" Margin="0,5,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <GroupBox Grid.Row="1" Header="Preset" Margin="0,0,0,10" Padding="10">
            <StackPanel Orientation="Horizontal">
                <ComboBox x:Name="cboPresets" Width="200" Height="28" FontSize="12"/>
                <Button Content="Load" Width="80" Height="28" Margin="10,0,0,0" Click="BtnLoadPreset_Click"/>
                <Button Content="Save" Width="80" Height="28" Margin="10,0,0,0" Click="BtnSavePreset_Click"/>
                <TextBox x:Name="txtPresetName" Width="150" Height="28" Margin="20,0,0,0" 
                         VerticalContentAlignment="Center" FontSize="12"/>
                <Button Content="Save As" Width="100" Height="28" Margin="10,0,0,0" Click="BtnSaveAsPreset_Click"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Fields" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="All Summary" Width="100" Height="26" Click="BtnAllSummary_Click" FontSize="11"/>
                    <Button Content="All Hidden" Width="90" Height="26" Margin="10,0,0,0" Click="BtnAllHidden_Click" FontSize="11"/>
                    <TextBlock Text="  |  " VerticalAlignment="Center" Foreground="#999"/>
                    <Button Content="+ Add Field" Width="90" Height="26" Margin="10,0,0,0" Click="BtnAddField_Click" FontSize="11"/>
                    <Button Content="Remove" Width="70" Height="26" Margin="5,0,0,0" Click="BtnRemoveField_Click" FontSize="11"/>
                    <TextBlock Text="  |  " VerticalAlignment="Center" Foreground="#999"/>
                    <Button Content="Up" Width="50" Height="26" Margin="10,0,0,0" Click="BtnMoveUp_Click" FontSize="11"/>
                    <Button Content="Down" Width="50" Height="26" Margin="5,0,0,0" Click="BtnMoveDown_Click" FontSize="11"/>
                    <TextBlock Text="  |  " VerticalAlignment="Center" Foreground="#999"/>
                    <Button Content="Reanalyze" Width="90" Height="26" Margin="10,0,0,0" Click="BtnReanalyze_Click" FontSize="11"/>
                </StackPanel>

                <DataGrid Grid.Row="1" 
                          x:Name="dgFields"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          Background="White"
                          RowHeight="32"
                          FontSize="12">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding Order}" Width="40" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Field Name" Binding="{Binding FieldName, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                        <DataGridTextColumn Header="Display Name" Binding="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                        <DataGridComboBoxColumn Header="Display Type" 
                                                 SelectedItemBinding="{Binding DisplayTypeString, UpdateSourceTrigger=PropertyChanged}" 
                                                 Width="100">
                            <DataGridComboBoxColumn.ItemsSource>
                                <x:Array Type="{x:Type sys:String}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                    <sys:String>Column</sys:String>
                                    <sys:String>Summary</sys:String>
                                    <sys:String>Hidden</sys:String>
                                </x:Array>
                            </DataGridComboBoxColumn.ItemsSource>
                        </DataGridComboBoxColumn>
                        <DataGridTextColumn Header="Width" Binding="{Binding ColumnWidth, UpdateSourceTrigger=PropertyChanged}" Width="60"/>
                        <DataGridTextColumn Header="Sample Values" Binding="{Binding SamplePreview}" Width="*" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#666"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Reset" Width="120" Height="32" Click="BtnReset_Click" FontSize="12"/>
            <Button Content="Cancel" Width="80" Height="32" Margin="10,0,0,0" Click="BtnCancel_Click" FontSize="12"/>
            <Button Content="Apply" Width="100" Height="32" Margin="10,0,0,0" 
                    Background="#4CAF50" Foreground="White" FontWeight="Bold"
                    Click="BtnApply_Click" FontSize="12"/>
        </StackPanel>
    </Grid>
</Window>
